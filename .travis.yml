language: python
python:
  - "3.5"

install:
  - pip install .
  - pip install codecov

script:
  - coverage run --source src,test test/main_test.py
  - coverage run --source src,test test/number_test.py

after_success:
  - codecov
